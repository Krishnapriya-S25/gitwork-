> git init=>[initialization of git]

>git status=> [list all untracked file]


>git add-A=>[adding of untracked files to staging area]


>git commit -m "git commit message"=>[to commit added file to local respository]


>git clone=>[to take exact copy from remote respository to local respository]

>git pull=>[to take changesfrom rmote respository to local workspace]


>git push=>[to sent changes from local respository to remote respository]


>git config --list =>[to list configuration]


>git config --global user.name "username"=>[setting user name]


>git config --global user.email "email"=>[setting email]